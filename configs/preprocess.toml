# Preprocess every subject recorded in the indexed dataset manifest.
dataset_dir = "data/indexed"

# Includes the original plus nine randomly augmented variants. This must be at
# least as large as the augmentation factors requested by training.
augmentation_factor = 10
resume = false